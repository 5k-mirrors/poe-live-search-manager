require "spec_helper"

RSpec.describe Poe::Sniper do
  it "has a version number" do
    expect(Poe::Sniper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
