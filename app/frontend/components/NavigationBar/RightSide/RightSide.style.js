import { makeStyles } from "@material-ui/core/styles";

export const useRightSideStyles = makeStyles({
  root: {
    position: "absolute",
    right: 10
  },
  button: {
    textTransform: "none",
    marginLeft: 10
  }
});
