import { makeStyles } from "@material-ui/core/styles";

export const useInfoButtonStyles = makeStyles({
  iconButton: {
    padding: 0,
    marginLeft: 5
  },
  helpIcon: {
    color: "#FFFFFF",
    fontSize: "35px"
  }
});
