import makeStyles from "@mui/styles/makeStyles";

export const useInputStyles = makeStyles({
  input: {
    color: "#FFFFFF",
  },
  label: {
    color: "#FFFFFF",
    "&.Mui-focused": {
      color: "#FFFFFF",
    },
  },
  focused: {},
});
