import makeStyles from "@mui/styles/makeStyles";

export const useInfoButtonStyles = makeStyles({
  iconButton: {
    padding: 0,
    marginLeft: 5,
  },
  helpIcon: {
    color: "#FFFFFF",
    fontSize: "35px",
  },
});
