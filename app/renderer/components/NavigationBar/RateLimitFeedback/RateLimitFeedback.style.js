import { makeStyles } from "@material-ui/core/styles";

export const useRateLimitFeedbackStyles = makeStyles({
  tooltip: {
    maxWidth: "none",
    fontSize: 14,
  },
  warningIcon: {
    color: "#F7A24D",
  },
  checkCircleIcon: {
    color: "#228B22",
  },
});
